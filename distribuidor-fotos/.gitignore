# Credenciais da sessão do WhatsApp — NUNCA versionar
auth/

# Configuração real (contém JIDs dos grupos das famílias)
config.json

# Fotos de referência dos alunos — dados pessoais, ficam só na máquina da escola
fotos-referencia/

# Modelos de reconhecimento facial (baixados via `npm run modelos`)
modelos/

# Cache de descritores faciais gerado pelo treino
descritores.json

# Histórico de envios (controle de fotos repetidas e limite diário)
historico.json

node_modules/
*.log
